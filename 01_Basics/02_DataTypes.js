const uid = 22010
let name = "Keshav"
let age = 20
let city = "madhubani"
let state;

console.table([uid, name, age, city, state])
console.table([typeof uid, typeof name, typeof age, typeof city, typeof state])